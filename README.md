def main():
    quarterly_list = [] 
    quarterly_list = [q1, q2, q3, q4]
    def quarterly_sales():
        q1 = float (input("Enter sales for Q1: ")) 
        q2= float (input ("Enter sales for Q2: "))
        q3 = float (input("Enter sales for Q3: "))
        q4 = float (input("Enter sales for Q4: "))
quarterly_sales()
    def total():
        quarterly_list = []
        quarterly_list = [ql, q2, q3, q4] 
        total = print ("Total: \t", sum (quarterly_list))
total()
    def average ():
        quarterly_list = []
        quarterly_list = [q%%!l, q2, q3, q4]
        mean = sum (quarterly_list) / len (quarterly_list) 
        average = print ("Average Quarter: \t", round (mean, 2))
    def minimum ():
        quarterly_list = []
        quarterly_list = [ql, q2, q3, q4]
        minimum = min(quarterly_list)
        lowest = round(minimum, 2)
        print ("Lowest Quarter: \t" + lowest)
minimum ()
    def maximum ():
        quarterly_list = []
        quarterly_list [ql, q2, q3, q4] 
        maximum = max (quarterly_list)
        highest =round (maximum, 2) 
        print ("Highest Quarter: \t" + highest)
maximum ()
print ("The Quarterly Sales Program" )
